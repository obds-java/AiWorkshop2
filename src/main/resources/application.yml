spring:
  application:
    name: ai-worskhop-2
  ai:
    vectorstore:
      weaviate:
        host: localhost:8080
        scheme: http
        api-key: your-weaviate-api-key
        initialize-schema: true

    openai:
        api-key: open-ai-api-key
